$("#course-classes-table").DataTable().draw(false);

<% if notice || alert %>
    $('.alert').remove();
    $('main .wrapper').find('.page-header').after("<%= j render 'general/flash_messages' %>");
<% end %>