<div role="tabpanel" class="tab-pane fade in" id="attendances">
  <div class="form-group">
    <%= link_to fa_icon('plus', text: t('mongoid.models.attendance.other')), new_course_class_attendance_path(@course_class), remote: true, class:'btn btn-default' %>
  </div>

  <table id="attendances-table" class="datatable table table-hover table-striped" data-source="<%= course_class_attendances_path(@course_class, format: :json) %>">
    <thead>
    <tr>
      <th><%= t('mongoid.attributes.attendance.date') %></th>
      <th class="text-center"><%= t('mongoid.attributes.attendance.present') %></th>
      <th class="text-center"><%= t('mongoid.attributes.attendance.absent') %></th>
      <th class="text-center"><%= t 'datatable.options' %></th>
    </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div><!--//students-->